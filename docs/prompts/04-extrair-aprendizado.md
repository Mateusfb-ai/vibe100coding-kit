# Prompt: registrar o aprendizado (fechar a tarefa)

```
Acabamos de resolver <problema>. Antes de seguir:

1. Skill extract-approach: nota em learnings/<data>-<slug>.md, escrita para um
   modelo mais fraco repetir o caminho (problema em uma linha, abordagem em
   passos, decisões de julgamento, regra reusável de uma linha).
2. Se virou regra durável: linha na tabela "erro → regra que previne" do
   CLAUDE.md, ou entrada em docs/DECISOES.md com a medição.
3. Se ficou gate novo: mutação em scripts/mutacoes.mjs e npm run meta-gate verde
   colado aqui.
4. Relatório curto em markdown para o dono: feito · no ar × só salvo · falta ·
   deu errado.
```
